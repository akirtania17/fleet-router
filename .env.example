# OpenRouteService API key.
# NOTE: legacy/optional. The current backend does not reference this key
# anywhere in app/ and uses haversine distances, so it is NOT required to
# run fleet-router. Leave empty unless you wire ORS in yourself.
ORS_API_KEY=
